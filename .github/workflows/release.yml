name: Release

# -----------------------------------------------------------------------------
# Tag-triggered npm publish is OFF until the package is on npm and NPM_TOKEN is set.
#
# To enable: uncomment `on`/`jobs` below and remove `workflow_dispatch` + `noop`.
#
# on:
#   push:
#     tags:
#       - "v*"
#
# permissions:
#   contents: read
#   id-token: write
#
# jobs:
#   publish:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - uses: actions/setup-node@v4
#         with:
#           node-version: "22"
#           registry-url: "https://registry.npmjs.org"
#           cache: npm
#       - run: npm ci
#       - run: npm run typecheck
#       - run: npm test
#       - run: npm run build
#       - name: Determine npm dist-tag
#         id: tag
#         run: |
#           if echo "${GITHUB_REF_NAME}" | grep -qE '-(testing|alpha|beta|rc)\.'; then
#             echo "dist_tag=testing" >> "$GITHUB_OUTPUT"
#           else
#             echo "dist_tag=latest" >> "$GITHUB_OUTPUT"
#           fi
#       - run: npm publish --access public --tag ${{ steps.tag.outputs.dist_tag }}
#         env:
#           NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
# -----------------------------------------------------------------------------

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow disabled — uncomment jobs when npm publishing is ready."
